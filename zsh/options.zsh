setopt interactivecomments

# Do not overwrite existing files with > and >>.
# Use >! and >>! to bypass
unsetopt CLOBBER

setopt auto_cd
setopt multios
setopt prompt_subst
setopt longlistjobs
setopt notify
